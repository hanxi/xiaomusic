# 🧭 开源贡献最佳实践：重置主分支 + 分离 PR 分支

## ✅ 最终目标

- **[B](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 分支**：干净地跟踪 `upstream/A`（你的主分支不再包含任何私有代码）。
- **[C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 分支**：保留你原始的开发状态（可用于本地继续开发或备份）。
- **[D](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 分支**：从 [C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 提炼出的**干净、线性、基于最新上游**的功能分支，**专门用于向上游提 PR**。

---

## 🧰 前提条件

- 你已添加上游 remote：
  ```bash
  git remote add upstream <上游仓库地址>
  ```

- 你当前在本地有分支 `B`、`C`，且 `B == C`（已合并过）。
- 你的所有功能代码都在 `C` 中（确认无丢失）。

---

## 🔧 完整执行流程（逐条复制即可）

```bash
# 1. 获取上游最新代码
git fetch upstream

# 2. 切换到 C 分支，确保它包含你全部开发内容
git checkout C

# 3. 【可选但推荐】先备份当前 C（以防万一）
git branch backup-C C

# 4. 创建新分支 D，用于 PR（此时 D = C）
git checkout -b D

# 5. 将 D rebase 到 upstream/A 上，形成干净线性历史
git rebase upstream/A
```


> ⚠️ **如果有冲突**：
> - 手动解决
> - `git add .`
> - `git rebase --continue`
> - 重复直到完成

```bash
# 6. 强制推送 D 到你的 fork（首次推送可用 -u）
git push -u origin D

# 7. 重置 B 分支为 upstream/A 的纯净状态（关键！）
git checkout B
git reset --hard upstream/A

# 8. 强制更新远程 B（⚠️ 确保没人依赖你的 B 分支）
git push --force-with-lease origin B

# 9. （可选）验证状态
git log --oneline -10   # 查看 B 是否等于 upstream/A
git checkout D
git log --oneline -10   # 查看 D 是否只有你的功能提交 + 最新 upstream
```


---

## 📤 后续操作

### ✅ 提交 PR

在 GitHub / GitLab 上：

- **Base repository**: upstream
- **Base**: A
- **Head repository**: your-fork
- **Compare**: D

提交 PR，描述清晰。

### 🔁 如果需要更新 PR（例如 reviewer 要求修改）

```bash
git checkout D
# 做修改
git add .
git commit -m "Address review feedback"
git push origin D   # 不需要 --force，除非你改了历史
```


### 🔁 如果上游 A 有新提交，需同步

```bash
git fetch upstream
git checkout D
git rebase upstream/A
git push --force-with-lease origin D
```


### 🧹 PR 合并后清理

```bash
# 删除本地和远程 D
git branch -d D
git push origin --delete D

# （可选）删除 backup-C
git branch -d backup-C
```


---

## ❓ 为什么还要创建 D？不能直接用 C 吗？

虽然技术上可以用 [C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4)，但因为你之前已将 [C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 合并进 [B](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4)，[C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 的历史可能包含：

- merge commit（如 "Merge C into B"）
- 与 [B](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 相同的冗余提交拓扑

而 [D](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 是全新提炼的线性分支，只包含必要提交，极大提高 PR 被接受的概率，也避免未来同步时 Git 误判历史。

💡 你可以把 [C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 当作"工作草稿"，[D](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) 当作"正式投稿"。

---

## ✅ 总结：你的分支角色

| 分支 | 用途 | 是否包含私有代码 | 是否推送到远程 | 是否用于 PR |
|------|------|------------------|----------------|-------------|
| [B](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) | 跟踪上游主干 | ❌ 否 | ✅ 是（作为干净镜像） | ❌ 否 |
| [C](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) | 本地开发 / 备份 | ✅ 是 | ✅ 可选 | ❌ 不推荐 |
| [D](file://C:\dev\boluofan\xiaomusic-online\xiaomusic\static\tailwind\libs\vue@3.5.13.js#L4-L4) | 专门 PR 分支 | ✅ 是（但干净） | ✅ 是 | ✅ 是 |

---

📌 这套流程是专业开源贡献的标准做法，既保证了协作规范，又保留了你的开发自由度。

✅ **建议保存此文档作为团队或个人贡献指南**。  
如有疑问，可通过 `git log --oneline --graph` 输出进一步分析分支状态。
